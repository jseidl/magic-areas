name: Run tests with latest stable Home Assistant

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

permissions:
  contents: read

jobs:
  test-stable:
    name: "Test against stable release"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Install tox and jq
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-test.txt
          sudo apt-get update && sudo apt-get install -y jq

      - name: "Run tests against stable"
        run: scripts/test stable tests/

  # test-beta:
  #   name: "Test against beta release"
  #   runs-on: ubuntu-latest
  #   continue-on-error: true
  #   steps:
  #     - uses: actions/checkout@v7

  #     - name: Set up Python
  #       uses: actions/setup-python@v7
  #       with:
  #         python-version: "3.14"

  #     - name: Install tox and jq
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements-test.txt
  #         sudo apt-get update && sudo apt-get install -y jq

  #     - name: "Run tests against beta"
  #       run: scripts/test beta tests/